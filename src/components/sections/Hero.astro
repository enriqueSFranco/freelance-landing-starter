---
import Badge from "@components/ui/Badge.astro";
import LinkButton from "@components/ui/LinkButton.astro";
import { site } from "@data/site";

---

<section id="hero" class="fade-in relative py-32 px-6 text-center max-w-4xl mx-auto">
  <Badge text="Barbería premium" />

  <h1 class="text-4xl mx-auto md:text-6xl font-semibold tracking-tight">
    {site.hero.title}
  </h1>

  <p class="mt-6 text-lg md:text-xl max-w-2xl mx-auto" style="color: var(--text-muted);">
    {site.hero.subtitle}
  </p>

  <!-- CTAs -->
  <div class="mt-10 flex justify-center gap-4">
    <LinkButton href={site.hero.ctaPrimary.link}>
      {site.hero.ctaPrimary.label}
    </LinkButton>

    <LinkButton href={site.hero.ctaSecondary.link} variant="secondary">
      {site.hero.ctaSecondary.label}
    </LinkButton>
  </div>
</section>


<style is:inline></style>